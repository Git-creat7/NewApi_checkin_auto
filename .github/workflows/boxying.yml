name: boxying-checkin

on:
  workflow_dispatch: # 支持手动点击运行
  schedule:
    # 北京时间 9:00-12:00 对应 UTC 1:00-4:00
    # 每天触发一次，再随机延迟 0-180 分钟
    - cron: '0 1 * * *'

jobs:
  checkin:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Random delay (0-180 min)
        run: |
          DELAY=$(python3 -c "import random; print(random.randint(0, 180))")
          echo "随机延迟 $DELAY 分钟"
          sleep $DELAY

      - name: Run Script
        env:
          # 从 GitHub Secrets 中读取敏感信息
          BOXYING_SESSION: ${{ secrets.BOXYING_SESSION }}
          BOXYING_API_USER: ${{ secrets.BOXYING_API_USER }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          # 如果你的脚本还需要其他变量（如 BASE_URL），也加在这里
          BOXYING_BASE_URL: "https://www.boxying.com/"
        run: python checkin/boxying/checkin.py
